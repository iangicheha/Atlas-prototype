# rosdep.yaml
# Custom rosdep keys for rbot packages.
# All standard ROS 2 dependencies are resolved from the official rosdep database.
# Add entries here only for non-standard system packages.
#
# Example:
# my_custom_dep:
#   ubuntu: [libmy-custom-dev]
